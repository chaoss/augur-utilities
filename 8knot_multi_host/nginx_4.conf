user  nginx;
events {
    worker_connections   1000;
}
http {
    server {
        listen 8094;
        access_log  /dev/null;
        location / {
            proxy_pass http://app-server_4:8094;
            proxy_read_timeout 600s;
        }
    }
}
