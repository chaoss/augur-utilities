user  nginx;
events {
    worker_connections   1000;
}
http {
    server {
        listen 8092;
        access_log  /dev/null;
        location / {
            proxy_pass http://app-server_2:8092;
            proxy_read_timeout 600s;
        }
    }
}
