user  nginx;
events {
    worker_connections   1000;
}
http {
    server {
        listen 8097;
        access_log  /dev/null;
        location / {
            proxy_pass http://app-server_7:8097;
            proxy_read_timeout 600s;
        }
    }
}
