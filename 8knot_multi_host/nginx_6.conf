user  nginx;
events {
    worker_connections   1000;
}
http {
    server {
        listen 8096;
        access_log  /dev/null;
        location / {
            proxy_pass http://app-server_6:8096;
            proxy_read_timeout 600s;
        }
    }
}
